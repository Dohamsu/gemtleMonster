# Required Environment Variables

# ============================================
# Supabase Configuration
# ============================================

# Your Supabase Project Reference ID
# Find it at: Dashboard → Project Settings → General → Reference ID
# Example: abcdefghijklmnop
SUPABASE_PROJECT_REF=

# Supabase API URL
# Find it at: Dashboard → Project Settings → API → Project URL
VITE_SUPABASE_URL=

# Supabase Anonymous Key (Public)
# Find it at: Dashboard → Project Settings → API → Project API keys → anon public
VITE_SUPABASE_ANON_KEY=

# Supabase Service Role Key (Private - Server-side only)
# Find it at: Dashboard → Project Settings → API → Project API keys → service_role
# ⚠️ NEVER expose this in client-side code
SUPABASE_SERVICE_ROLE_KEY=

# ============================================
# Optional: For Type Generation with Auth
# ============================================

# Personal Access Token (for remote type generation)
# Generate at: Dashboard → Account → Access Tokens
# Only needed if using `npm run types:generate`
# SUPABASE_ACCESS_TOKEN=
